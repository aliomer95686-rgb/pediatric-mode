import React from "react";
import ReactDOM from "react-dom/client";
import PediatricModeApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PediatricModeApp />
  </React.StrictMode>
);
